const fs = require('fs'); const path = require('path'); console.log('Building...'); const publicDir = path.join(__dirname, 'public'); if (!fs.existsSync(publicDir)) { fs.mkdirSync(publicDir, { recursive: true }); } const htmlContent = '<!DOCTYPE html><html><head><title>Advancia PayLedger</title></head><body><h1>Advancia PayLedger</h1><p>Global Financial Platform</p></body></html>'; fs.writeFileSync(path.join(publicDir, 'index.html'), htmlContent); console.log('Done!');
